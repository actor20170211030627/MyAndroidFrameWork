<resources>
    <!--                             Base.Theme.AppCompat.Light.Dialog -->
    <style name="BaseDialogTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <!-- 去掉标题栏 -->
        <item name="windowNoTitle">true</item>

        <!-- 无边框 -->
        <item name="android:windowFrame">@null</item>

        <!-- 背景透明: 去掉弹窗背景(原生背景带有外边距), 改为透明色, 如果不改, Dialog有一个默认padding -->
        <item name="android:windowBackground">@android:color/transparent</item><!--#0011dddd-->

        <!-- 浮在 Activity 之上 -->
        <item name="android:windowIsFloating">true</item>

        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>

        <!--全屏, 作用未知-->
        <item name="android:windowFullscreen">true</item>

        <!-- 窗口内容不覆盖, 作用未知 -->
        <item name="android:windowContentOverlay">@null</item>

        <!-- 遮盖层: 显示区域以外是否使用黑色半透明背景 -->
        <item name="android:backgroundDimEnabled">true</item>



        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>

        <!-- 动画效果 -->
<!--        <item name="android:windowEnterAnimation">@anim/window_scale_in</item>-->
<!--        <item name="android:windowExitAnimation">@anim/window_scale_out</item>-->

        <!-- Base.Theme.AppCompat.Light.Dialog.Alert 特有的属性，用于指定默认宽度，这里需要禁用 -->
        <item name="android:windowMinWidthMajor">0dp</item>
        <item name="android:windowMinWidthMinor">0dp</item>

        <!-- 解决 ActionBar 占位导致 Dialog 无法全屏显示的问题 -->
        <item name="windowActionBar">false</item>

        <!-- ActionMode 覆盖 Actionbar，不顶下来 -->
        <item name="windowActionModeOverlay">true</item>

        <!-- 不感知敏感的切换 -->
        <!--<item name="android:configChanges">orientation|screenSize|keyboardHidden</item>-->
        <!-- 默认隐藏输入法 -->
        <!--<item name="android:windowSoftInputMode">stateHidden</item>-->
        <!-- Dialog 全屏显示 -->
        <!--<item name="android:windowFullscreen">true</item>-->
    </style>
</resources>